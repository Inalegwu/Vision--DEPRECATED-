import { styled } from "../../stitches.config";

export const Button = styled("button", {
  appearance: "none",
  border: "none",
  background: "none",
  outline: "none",
  transition: "0.2s ease-in-out",
  cursor: "pointer",
  letterSpacing:0.3,
  fontWeight:"lighter"
});
